---
import Index from './index.astro';
import { obtainStore } from '#src/posts';

const posts = await obtainStore().postsAsync({ order: 'asc' });
---
<Index title="Posts in asc order" posts={posts} />
